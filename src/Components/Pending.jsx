
const Pending = () => {
    return (
        <div>
          <h1 className="text-center text-2xl ">Now I am working two projects but then are not completed yet after completed here will be the updated</h1>
        </div>
    );
};

export default Pending;